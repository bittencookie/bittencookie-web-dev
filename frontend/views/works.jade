header-directive

section.work(
        ng-repeat="(key, work) in works", 
        style="{{work.background}}",
        ng-click="goToWork(key)"
)
    div.work-overlay
        h1.work-name(ng-bind="work.logo")
footer-directive
